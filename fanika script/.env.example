# IPRoyal residential proxy (Fanika) — copy to .env and fill in
PROXY_HOST=geo.iproyal.com
PROXY_PORT=11231
PROXY_USERNAME=your_proxy_username
PROXY_PASSWORD=your_proxy_password

# Sticky session lifetime for IPRoyal sticky sessions
PROXY_LIFETIME=30m

# Optional country code (e.g. ma, es). Leave empty if already in PROXY_PASSWORD.
PROXY_COUNTRY=

# Keep rotating until public IP actually changes
PROXY_ROTATE_MAX_ATTEMPTS=8
PROXY_ROTATE_RETRY_MS=800
